/**
 * Replace with your workspace slug
 */
export const slug = 'WORKSPACE_SLUG';

/**
 * Replace with your cycle token
 * @see https://docs.cycle.app/the-graphql-api/authentication#create-a-personal-api-key
 */
export const token = 'TOKEN';

/**
 * Cycle endpoint
 */
export const graphqlEndpoint = 'https://api.product.cycle.app/graphql';
